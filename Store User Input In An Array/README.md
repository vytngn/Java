Take user input of multiple integers and then display the list as an array. 
Compare each method from simple to more complex.
What are the pros and cons ? And what is the time complexity of each one ? 
